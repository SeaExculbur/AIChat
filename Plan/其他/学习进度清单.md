# 学习进度清单

> 项目：AIChat v1.0
> 开始：2026-06-09
> 当前阶段：前端环境搭建完成（2026-06-19），Vue 3 骨架就绪，通信链路认知地图已建，准备写路由

---

## 一、Git 版本控制

| 知识点 | 熟练度 |
|--------|--------|
| `git init` / `git add` / `git commit` | 熟练 |
| `git status` / `git log` / `git show` | 熟练 |
| `git commit --amend` / `--reset-author` / `--no-edit` | 熟练 |
| 分支策略（main → dev → feature/xxx） | 熟练 |
| `git checkout -b` / `git switch -c` | 熟练 |
| `git push` / `git pull` / `git fetch` | 熟练 |
| `git push --force`（fast-forward 被拒绝时） | 理解 |
| `git remote add origin` / `git remote -v` | 熟练 |
| `.gitignore`（路径规则、通配符、`!` 例外） | 熟练 |
| `git reset HEAD`（清暂存区） | 会用 |
| `git restore --staged <file>` | 会用 |
| LF/CRLF 换行符警告 | 理解 |
| GitHub 邮箱隐私保护 + 匿名邮箱 | 理解 |
| Git 存储机制（commit/tree/blob 对象） | 理解 |
| commit hash 不可篡改链 | 理解 |
| 二进制文件与 Git 不兼容的原因 | 理解 |
| 合并冲突规则（同文件不同行/同行/相邻行） | 理解 |
| fast-forward 检查 | 理解 |
| push 底层流程（协商→打包→传输→解压） | 理解 |
| delta compression / packfile | 了解 |
| PR 流程（fork → clone → branch → commit → PR → review） | 了解 |
| PR 模板（`.github/PULL_REQUEST_TEMPLATE.md`） | 了解 |

---

## 二、Flask 后端框架

| 知识点 | 熟练度 |
|--------|--------|
| Flask 本质 = URL → 函数的映射字典 | 理解 |
| `app = Flask(__name__)` | 熟练 |
| `app.route()` = 往路由表加一行 | 熟练 |
| 工厂模式 `create_app()` | 熟练 |
| 蓝图（Blueprint）= 可拆卸独立路由表 | 熟练 |
| `app.register_blueprint(auth_bp)` | 熟练 |
| `app.config.from_object(Config)` | 熟练 |
| `app.run(debug=True)` 与 debug 安全风险 | 理解 |
| Werkzeug = HTTP 解析器 + 开发服务器（独立于 Flask） | 理解 |
| Flask 与 Werkzeug 的关系 | 理解 |
| WSGI = Web 服务器与 Python 应用的通用插头 | 理解 |
| Werkzeug vs Gunicorn（开发 vs 生产） | 理解 |
| 所有 Web 框架本质一样（Flask/FastAPI/Spring Boot） | 理解 |
| 请求上下文（request context） | 理解 |
| 应用上下文（app context） | 理解 |
| `with app.app_context()` 栈机制（push/pop） | 理解 |
| 上下文本质 = 线程局部变量的标记位 | 理解 |
| Flask 钩子（before_request / after_request） | 了解 |
| `@jwt_required()` = 局部钩子 | 理解 |
| HTTP 请求完整生命周期（TCP→Werkzeug→Flask→业务函数→Response→TCP） | 理解 |
| `app.py` 本质 = 组装，不是实现 | 理解 |
| 流式响应（`yield` 生成器） | 熟练 |
| SSE 协议（`data:` 格式、`text/event-stream`） | 会用 |
| `Response(生成器, mimetype="text/event-stream")` | 会用 |
| SSE vs 普通 JSON 响应（Content-Length vs chunked） | 理解 |
| `request.args.get()` — URL 查询参数读取 | 会用 |
| `paginate()` — SQLAlchemy 分页 | 会用 |
| `m.created_at.isoformat()` — datetime 序列化 | 会用 |
| 列表推导式（ORM 对象 → dict 列表） | 熟练 |
| `desc()` / `asc()` — 排序方向选择 | 会用 |

---

## 三、数据库（SQLAlchemy + SQLite）

| 知识点 | 熟练度 |
|--------|--------|
| ORM 本质（Python 类 ↔ 数据库表） | 理解 |
| `db = SQLAlchemy()` | 熟练 |
| `class User(db.Model)` | 熟练 |
| `db.Column()` 参数（primary_key, unique, nullable, server_default, onupdate, ForeignKey） | 熟练 |
| `__tablename__` | 熟练 |
| `db.create_all()`（建表，不更新已有表） | 熟练 |
| `db.init_app(app)` | 熟练 |
| `db.session.add()` / `commit()` / `delete()` | 熟练 |
| `User.query.get(id)`（主键查询） | 熟练 |
| `User.query.filter_by(col=val).first()` | 熟练 |
| `Model.query.filter_by(...).delete()`（批量删除） | 会用 |
| `.order_by()` / `.limit()` / `.all()` | 熟练 |
| `.paginate()` — 分页（内部发 COUNT + LIMIT/OFFSET 两条 SQL） | 会用 |
| `index=True` — 手动建索引（B+ 树） | 会用 |
| `request.get_json()` / `data.get("key")` | 熟练 |
| 密码哈希（`generate_password_hash` / `check_password_hash`） | 熟练 |
| 外键（`ForeignKey`）= 数据一致性 | 理解 |
| 外键的级联删除（`ondelete="CASCADE"`） | 了解 |

---

## 四、数据库索引与 B+ 树

| 知识点 | 熟练度 |
|--------|--------|
| B+ 树结构（根→内部节点→叶子双向链表） | 理解 |
| B+ 树查找 = 逐层二分，O(log n) | 理解 |
| 节点分裂/合并（保持平衡） | 理解 |
| 节点大小 = 磁盘页大小（I/O 优化） | 理解 |
| 聚簇索引（主键，叶子存整行） | 理解 |
| 非聚簇索引（叶子只存索引列 + 主键 id） | 理解 |
| 回表（从非聚簇拿到主键，再去聚簇查完整行） | 理解 |
| 为什么一张表需要多棵 B+ 树（一种排序 = 一棵树） | 理解 |
| INSERT/UPDATE/DELETE 各索引的更新时机 | 理解 |
| 聚簇 vs 非聚簇：改密码不动 username 树 | 理解 |
| 全表扫描触发条件（无索引、索引列运算、LIKE '%x%'、数据太小） | 理解 |
| 查询优化器（基于统计信息选执行路径） | 理解 |
| 统计信息过期导致优化器选错 | 理解 |
| `ANALYZE` 手动更新统计 | 了解 |
| `index=True` 建索引 — 从理论到工程的第一次跨层应用 | 用上了 |

---

## 五、JWT 鉴权与安全

| 知识点 | 熟练度 |
|--------|--------|
| JWT 三段结构（Header.Payload.Signature） | 熟练 |
| 签名计算过程（Payload + JWT_SECRET_KEY → 哈希） | 理解 |
| 签名验证过程（重算→比对） | 理解 |
| `create_access_token(identity=...)` | 熟练 |
| `get_jwt_identity()` | 熟练 |
| `@jwt_required()` 装饰器底层（闭包 + 拦截 + 线程局部变量） | 理解 |
| 注册/登录为什么不需要 `@jwt_required()`（尚未拿到 token） | 理解 |
| JWT 无服务端登出机制 | 理解 |
| JWT vs Session 对比 | 理解 |
| 为什么选 JWT（多服务器无需 Redis） | 理解 |
| 密码哈希不可逆 | 理解 |
| 穷举 JWT 密钥数学上不可能（SHA-256） | 理解 |
| 防枚举攻击（登录不区分用户名错/密码错） | 理解 |
| 注册接口为什么明示"用户名已存在" | 理解 |
| HTTPS 防中间人抓包 | 了解 |
| XSS 偷 localStorage token | 了解 |

---

## 六、HTTP 协议与 API

| 知识点 | 熟练度 |
|--------|--------|
| HTTP ≠ HTML | 熟练 |
| HTTP 方法（GET/POST/DELETE） | 熟练 |
| 状态码（200/201/400/401/409/500） | 熟练 |
| JSON = 前后端通用数据格式 | 熟练 |
| curl = 命令行里的浏览器 | 熟练 |
| curl 参数（-X / -H / -d） | 熟练 |
| PowerShell 的 curl 是假的（`Invoke-WebRequest`） | 知道 |
| API = 跨网络的函数签名 | 理解 |
| 后端定 URL，前端照着写（API 契约） | 理解 |
| GET 是安全的（不改变服务器状态），POST 不幂等 | 理解 |
| 浏览器地址栏永远发 GET | 理解 |

---

## 七、Python 基础

| 知识点 | 熟练度 |
|--------|--------|
| 类 vs 实例（`User` vs `user`） | 熟练 |
| 装饰器（`@app.route` / `@jwt_required`） | 熟练 |
| 上下文管理器（`with` / `__enter__` / `__exit__`） | 理解 |
| `os.getenv("KEY", "默认值")` | 熟练 |
| `load_dotenv()` / `.env` 文件 | 熟练 |
| 环境变量优先级（系统 > .env > 默认值） | 理解 |
| Python 装饰器链执行顺序（从下往上） | 理解 |
| `int()` / `str()` 类型转换 | 熟练 |
| Wrkzeug 的 `werkzeug.security` 模块 | 会用 |
| 线程局部变量（`threading.local`） | 理解 |
| Python 解释型语言执行机制（逐行读取执行，`def` 不执行函数体） | 理解 |
| 循环 import 检测机制（返回半成品模块） | 理解 |
| `__call__` 方法（让实例可像函数一样调用） | 理解 |
| `yield` 生成器（暂停/恢复，打字机效果） | 熟练 |
| 生成器与 `return` 的区别（送完不结束 vs 送完结束） | 熟练 |
| 列表推导式（`[... for x in list]`） | 熟练 |
| 变量重构纪律（改名 → 全局搜旧名） | 踩过坑 |
| 副作用路径验证（curl 返回 200 ≠ 数据库正确） | 踩过坑 |

---

## 八、WSGI 与通信底层

| 知识点 | 熟练度 |
|--------|--------|
| WSGI = Web 服务器与 Python 应用的接口规范（PEP 3333） | 理解 |
| `environ` 字典（服务器解析 HTTP 后生成，传给 app） | 理解 |
| `start_response` 回调函数（服务器传入，app 调用它传递状态码和响应头） | 理解 |
| 为什么元数据和数据分两路返回（HTTP 协议要求头在体之前，SSE 流式需要） | 理解 |
| Response 对象的 `__call__` 是 WSGI 出口 | 理解 |
| 服务器没拿到 Response 对象——它通过 `start_response` 和 `return` 拿到状态码/头/体 | 理解 |
| Werkzeug 双重身份（工具包 + 开发服务器，生产环境摘掉通信层） | 理解 |
| Flask 源码里的 Werkzeug 证据（Response 继承、路由引擎、开发服务器） | 看过 |
| `request.get_json()` 是通过线程局部存储的代理读取 Werkzeug Request 对象 | 理解 |
| `@jwt_required()` 和 `get_jwt_identity()` 通过线程局部存储交换数据 | 理解 |

---

## 九、TCP 与 HTTP 协议底层

| 知识点 | 熟练度 |
|--------|--------|
| TCP 三次握手（SYN → SYN-ACK → ACK） | 理解 |
| TCP 四次挥手（FIN → ACK → FIN → ACK）——双向关闭，不能合并 | 理解 |
| 少了握手 → 连接建立失败；少了挥手 → 半开连接，资源不回收 | 理解 |
| HTTP 响应结构（状态行 + 响应头 + 空行 + 响应体） | 熟练 |
| `Content-Length`（定长响应）vs `Transfer-Encoding: chunked`（流式响应） | 理解 |
| `Content-Length` 被篡改 → 响应体截断 → JSON 解析失败 | 理解 |
| HTTPS 不仅加密响应体——也保护 `Content-Length` 等元数据完整性 | 了解 |

---

## 十、Python 执行机制与工程影响

| 知识点 | 熟练度 |
|--------|--------|
| Python 逐行读取执行（`def` 创建函数对象但不执行函数体） | 理解 |
| `if __name__ == "__main__"` 只是普通的 if 语句——读到时执行判断 | 理解 |
| Python vs C：解释型 vs 编译型——执行单元大小不同 | 理解 |
| 循环 import 链（auth → models → app → auth） | 理解 |
| 循环 import 导致路由静默消失（无报错、无堆栈、永久 404） | 发现过 |
| 装饰器在函数定义那一刻执行——早于任何 import 链里的 `register_blueprint` | 理解 |
| 写在函数下面的 `add_url_rule` 依赖人的纪律——纪律会被时间打破 | 理解 |
| 装饰器依赖 Python 解释器本身的执行时序——不可破 | 理解 |
| 解耦策略：模块引用方向保持单向（app → auth → models，不回头） | 理解 |

---

## 十一、工程实践

---

## 八、工程实践

| 知识点 | 熟练度 |
|--------|--------|
| 环境变量与 `.env` 安全性 | 理解 |
| `.env.example` 模板文件 | 了解 |
| 关注点分离（app.py 组装，auth.py 实现） | 理解 |
| `requirements.txt`（`>=` vs `==`） | 熟练 |
| 清华镜像源 pip install | 熟练 |
| Git commit 规范（feat/fix/chore/docs） | 熟练 |

---

## 十二、前端环境与工具链（2026-06-19 开始）

| 知识点 | 熟练度 |
|--------|--------|
| Node.js / npm | 会用 |
| `npm create vite@latest` — 脚手架创建项目 | 会用 |
| `package.json`（前端版 `requirements.txt` + scripts） | 会用 |
| `node_modules/`（前端版 `site-packages`） | 理解 |
| SPA（单页应用）：整个网站只有一个 HTML 文件 | 理解 |
| `index.html` — 浏览器入口，`<div id="app">` 空壳 | 理解 |
| Vite 双重身份：开发服务器 + 构建工具 | 理解 |
| Vite 热更新（HMR）：WebSocket 通知 → 局部替换组件 | 理解 |
| Vite 构建 vs C 编译类比 | 理解 |
| `vite.config.js` 逐行理解 | 会用 |
| `@vitejs/plugin-vue` — 让 Vite 读懂 `.vue` 文件 | 理解 |
| 代理：Vite 中转 `/api` 请求到 Flask | 理解 |
| 代理的 Python 等价实现 | 理解 |
| CORS：开发靠代理，生产靠 `CORS(app)` | 理解 |

## 十三、通信协议（加深）

| 知识点 | 熟练度 |
|--------|--------|
| 裸 TCP 能通信但需要约定格式 | 理解 |
| HTTP 协议存在的根本原因 | 理解 |
| 协议 = 中间约定层（HTTP / WSGI / ORM 统一模式） | 理解 |
| WebSocket vs HTTP（全双工 vs 一问一答） | 理解 |
| SSE vs WebSocket：聊天不需要双向 | 理解 |

## 十四、前端核心概念（初步了解）

| 知识点 | 熟练度 |
|--------|--------|
| DOM 事件系统：浏览器 `while True` 等用户动作 | 了解 |
| DOM 树：HTML 标签 → 内存节点树 | 了解 |
| `v-model`：输入框 ↔ 变量自动同步 | 了解 |
| 响应式：变量变 → 页面自动变 | 了解 |
| `@click`：用户动作 → 函数调用 | 了解 |
| Axios：前端版 curl——构造 HTTP 报文 | 了解 |
| JS 模块：`import` / `export default` | 会用 |

## 十五、文件结构

| 目录/文件 | 作用 |
|----------|------|
| `src/main.js` | 入口 |
| `src/App.vue` | 根组件 |
| `src/router/` | 路由表 |
| `src/views/` | 页面 |
| `src/components/` | 可复用组件 |
| `src/api/` | Axios 封装 |
| `src/store/` | Pinia 状态 |

---

## 当前项目文件

```
frontend/
├── package.json        ✅
├── vite.config.js      ✅ 代理已配置
├── index.html          ✅
└── src/
    ├── main.js         ✅
    ├── App.vue         ✅
    ├── api/            📁
    ├── components/     📁
    ├── router/         📁
    ├── store/          📁
    └── views/          📁

backend/
├── models.py          ✅ User + ChatHistory（user_id 索引已建）
├── config.py          ✅ Config 类
├── app.py             ✅ 入口 + 蓝图注册（auth + chat）
├── auth.py            ✅ 注册/登录/删除/修改密码
├── chat.py            ✅ 流式聊天 + 历史分页
├── requirements.txt   ✅
└── .env               ✅

已实现接口（6/6）：
POST   /api/auth/register       ✅
POST   /api/auth/login          ✅
DELETE /api/auth/delete-account ✅
POST   /api/auth/change-password ✅
POST   /api/chat                ✅
GET    /api/history             ✅

Plan/
├── Flask后端搭建.md
├── Flask业务接口_auth.md
├── Flask业务接口_chat.md
├── Git初始化复盘.md
├── 数据库与B+树.md
├── 通信的最底层逻辑链条.md
├── 学习进度清单.md
├── 开发上线流程.md
├── 前端工作链路.md                 ← 新增（键盘→HTTP 全链路）
├── 前端环境与通信概念.md            ← 新增（工具链/协议/代理）
└── test/
    ├── 后端基础自测.md
    ├── 后端基础自测_参考答案.md
    ├── 全栈基础自测_v2.md
    ├── 全栈基础自测_v2_参考答案.md
    └── B+树自述.md
```
